/**
 * Created by songxg on 16/6/23.
 */
const initState = {
    page: 'purchase',
    menuList: []
}

function purchase (state = initState, action) {
    switch (action.type) {
        default:
            return state;
    }
};

export default purchase;